import React from 'react'

 function Compo5() {
  return (
    <React.Fragment>
        <div>
        <h1 style={{textDecoration:"underline"}}>Components in React.JS</h1>
        <ul>
            <li>Components are a re-usable block of code that are used in React.js to create the User Interface (UI).</li>
            <li>Components are divided into multiple blocks of UI that are responsible for mounting and unmounting data on a web page.</li>
            <li>To create a component in React.JS we need to create a JSX file.</li>
            <li>"JSX" stands for JavaScript XML.</li>
            <li>A JSX file can be used to execute HTML, CSS, and JavaScript code in a single file.</li>
        </ul>
        </div>
        <br />
        <div>
            <h2>Types of Components</h2>
        </div>

    </React.Fragment>
  )
}
export default Compo5
